extends layout

block content
  div(class="sticker-card")
    h2= title
    p(class="flow-text ")  #{body}
    div(class="row")
      div(class="col s12 m3")
        div(class="card")
          div(class="card-image")
            img(src=image)


