extends layout

block content
  div(class="card")
    h2 #{pageTitle}
    div(class="container")
      div(class="row")
        div(class="col s12 m7")
          div(class="card")
            div(class="card-image")
              img(src=image)
              span(class="card-title")= image-title
      p(class="flow-text ")  #{body}

